### Triage

**You own the knife. Throughput over completeness.**

1. Confirm the current mission and constraint from the card. No card, run **Name the constraint** on its shortest path first: mission, constraint, falsifier.
2. Tag every item `relieves`, `noise`, or `unclear`. Judge each against the constraint mechanism, not against how good the item is on its own.
3. Every `unclear` gets one question or one falsifier ask this turn. Do not hand back a pile of unclears.
4. Order only the `relieves` items. Cap active work at one relief move. The owner may raise the cap, and then you state the cost to constraint relief in the same breath (**principle-one-relief-move**).
5. Recommend kill or park for every `noise` item with one sentence why. Parked means a named list with a revisit date, not "later".
6. An item that is cheap, nearly done, or already promised is still `noise` when it does not touch the mechanism. Say so and let the owner overrule you.

**Reply:** the card, then the tagged list, then the kill and park counts.
